server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: microservice
          uri: https://github.com/diegohuapayarivera/foodfix
          search-paths: config-data
        bootstrap: true
  application:
    name: config-service