server:
  port: 8002
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: https://eurekaservicefoodfix.herokuapp.com/eureka
  instance:
    instance-id: ${spring.application.name}:${server.port}